import { PageHeader, Empty } from "@/components/ui";

export default function PathsPage() {
  return (
    <div data-testid="page-paths">
      <PageHeader title="Paths" sub="This view is being built." />
      <Empty>Placeholder — the paths feature will render here.</Empty>
    </div>
  );
}
