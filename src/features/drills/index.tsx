import { PageHeader, Empty } from "@/components/ui";

export default function DrillsPage() {
  return (
    <div data-testid="page-drills">
      <PageHeader title="Drills" sub="This view is being built." />
      <Empty>Placeholder — the drills feature will render here.</Empty>
    </div>
  );
}
