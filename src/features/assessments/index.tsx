import { PageHeader, Empty } from "@/components/ui";

export default function AssessmentsPage() {
  return (
    <div data-testid="page-assessments">
      <PageHeader title="Assessments" sub="This view is being built." />
      <Empty>Placeholder — the assessments feature will render here.</Empty>
    </div>
  );
}
